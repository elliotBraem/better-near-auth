[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter better-near-auth build", "pnpm --filter @b2s/server build"]

[start]
cmd = "cd examples/browser-2-server/apps/server && pnpm start"
